<%- include("partials/header") %>

    <h1>Home</h1>
    <p> <%=homeContent%> </p>

    <% postContents.forEach(function(content) { %>
        <h1> <%= content.title %> </h1>
        <p> <%= content.contents.substring(0, 100) + " ..." %> <a href="/post/<%= content._id %>">Read more</a></p>
    <% }); %>


<%- include("partials/footer") %>